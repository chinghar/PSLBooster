{
  "name": "facial-analysis-app-setup",
  "private": true,
  "description": "Used only to fetch @mediapipe/tasks-vision locally during setup. Not served or required at runtime.",
  "dependencies": {
    "@mediapipe/tasks-vision": "^0.10.20"
  }
}
